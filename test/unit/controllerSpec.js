'use strict';

describe('Yellowhammer controller', function() {

  it('should be able to test with karma and jasmine now', function() {
    var a = 1;
    var b = 2;

    expect(b).toBeGreaterThan(a);
  })
});